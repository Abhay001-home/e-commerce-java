package com.ecommerce.entity;

/**
 * ShipmentStatus enum — tracking physical delivery stages.
 */
public enum ShipmentStatus {
    PENDING,
    DISPATCHED,
    IN_TRANSIT,
    DELIVERED,
    RETURNED
}
